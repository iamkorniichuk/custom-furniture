import { TranslatedPipe } from './translated-pipe';

describe('TranslatedPipe', () => {
  it('create an instance', () => {
    const pipe = new TranslatedPipe();
    expect(pipe).toBeTruthy();
  });
});
